import { ListNavigatorDirective } from './list-navigator.directive';

describe('ListNavigatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ListNavigatorDirective();
    expect(directive).toBeTruthy();
  });
});
